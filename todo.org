* Inbox
** TODO Add better unix tools as common modules for Mac and Linux
** TODO use home-manager in user install instead of system install
https://dzone.com/articles/nixos-home-manager-on-native-nix-flake-installatio
https://github.com/mudrii/hmtst
* Improvements
** DONE Move remote-notmuch to doom.d or as nix script
[[file:~/.dotfiles/machines/macbook/doom.d/config.el::notmuch-command "~/.emacs.d/bin/remote-notmuch.sh"))]]
** DONE Přidat do emacs vterm modul
** KILL Install vscode with remote-ssh on Macbook

- Note taken on [2021-10-30 so 16:17] \\
  I tried to do it according to the issue and VSCode wiki page, it did not work anyway and it is still necessary to update the node.

To make the server work without problems, see https://github.com/nix-community/home-manager/issues/939
** TODO Make sure to install direnv with flakes support using home-manager

This works from cmdline: ~nix-env -f '<nixpkgs>' -i -E 'pkgs: (pkgs {}).nix-direnv.override { enableFlakes = true; }'~
